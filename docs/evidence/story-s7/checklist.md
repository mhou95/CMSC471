# Story S7 — Step Functions Executes FetchImage → CallTextract → SaveResults

**Azure DevOps AB#:** (fill in)
**Feature file:** `tests/acceptance/features/orchestration_tier.feature` — "Verify Step Functions state machine exists"

## Audit Trail Checklist

- [ ] `requirement.png` — screenshot of DevOps story S7 in *Done* state
- [ ] `pytest_output.txt` — output of `pytest tests/acceptance -v -k "state machine"` showing PASSED
- [ ] `commit_link.txt` — GitHub link to commit with `Fixes AB#<id>` for this story
